// @index(['./assets/svg1/**/*.svelte'], (f, _, e) => `import Icon${_.pascalCase(f.name)} from '${f.path}${f.ext}';`)

// @endindex


// @index(['./routes/**/*.svelte', './templ/*.js'], (f, _, e) => `import ${_.pascalCase(f.name)} from '${f.path}${f.ext}';`)

// @endindex